apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aegis-minio-mvp
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  sources:
    - repoURL: https://charts.bitnami.com/bitnami
      chart: minio
      targetRevision: 14.6.0
      helm:
        valueFiles:
          - $values/kubernetes/envs/mvp/infrastructure/minio/values.yaml
    - repoURL: https://github.com/Aegis-AI-Organizations/Aegis-AI-Infra.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: aegis-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
