apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aegis-temporal-mvp
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://temporalio.github.io/helm-charts
    chart: temporal
    targetRevision: 0.74.0
    helm:
      valuesObject:
        server:
          replicaCount: 1
          config:
            persistence:
              defaultStore: default
              visibilityStore: visibility
              default:
                driver: "sql"
                sql:
                  driver: "postgres12"
                  host: "aegis-postgres-mvp-postgresql"
                  port: 5432
                  user: "aegis_admin"
                  existingSecret: "aegis-temporal-mvp-default-store"
                  secretKey: "password"
                  database: "temporal"
              visibility:
                driver: "sql"
                sql:
                  driver: "postgres12"
                  host: "aegis-postgres-mvp-postgresql"
                  port: 5432
                  user: "aegis_admin"
                  existingSecret: "aegis-temporal-mvp-default-store"
                  secretKey: "password"
                  database: "temporal_visibility"
        frontend:
          additionalEnv:
            - name: TEMPORAL_CONFIG_TEMPLATE
              value: "/etc/temporal/config/config_template.yaml"
        history:
          additionalEnv:
            - name: TEMPORAL_CONFIG_TEMPLATE
              value: "/etc/temporal/config/config_template.yaml"
        matching:
          additionalEnv:
            - name: TEMPORAL_CONFIG_TEMPLATE
              value: "/etc/temporal/config/config_template.yaml"
        worker:
          additionalEnv:
            - name: TEMPORAL_CONFIG_TEMPLATE
              value: "/etc/temporal/config/config_template.yaml"
        schema:
          setup:
            enabled: true
          update:
            enabled: true
        prometheus:
          enabled: false
        grafana:
          enabled: false
        cassandra:
          enabled: false
        postgresql:
          enabled: false
        elasticsearch:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: aegis-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
